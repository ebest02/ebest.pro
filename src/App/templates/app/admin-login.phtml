<?php $this->layout('layout::default', ['title' => 'Connexion Administration - ebest.pro']) ?>

<div class="row justify-content-center">
    <div class="col-md-6 col-lg-4">
        <div class="card shadow-soft border-light">
            <div class="card-body">
                <h1 class="h3 mb-4 text-center">Administration</h1>
                
                <?php if (isset($error) && $error): ?>
                    <div class="alert alert-danger" role="alert">
                        <?= $this->e($error) ?>
                    </div>
                <?php endif; ?>

                <form method="POST" action="/admin/login">
                    <div class="form-group mb-3">
                        <label for="email">E-mail</label>
                        <input type="email" class="form-control" id="email" name="email" required autofocus
                               value="<?= isset($email) ? $this->e($email) : '' ?>">
                    </div>
                    <div class="form-group mb-3">
                        <label for="password">Mot de passe</label>
                        <input type="password" class="form-control" id="password" name="password" required>
                    </div>
                    <button type="submit" class="btn btn-primary w-100 mb-2">
                        Se connecter
                    </button>
                    <a href="/admin/forgot-password" class="btn btn-link btn-sm p-0">Mot de passe oublié ?</a>
                </form>
            </div>
        </div>
    </div>
</div>
