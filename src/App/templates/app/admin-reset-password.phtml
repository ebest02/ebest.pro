<?php $this->layout('layout::default', ['title' => 'Nouveau mot de passe - ebest.pro']) ?>

<div class="row justify-content-center">
    <div class="col-md-6 col-lg-4">
        <div class="card shadow-soft border-light">
            <div class="card-body">
                <h1 class="h3 mb-4 text-center">Nouveau mot de passe</h1>

                <?php if (isset($success) && $success): ?>
                    <div class="alert alert-success" role="alert">
                        Votre mot de passe a été réinitialisé. Vous pouvez vous connecter.
                    </div>
                    <a href="/admin/login" class="btn btn-primary w-100">Se connecter</a>
                    <?php return; ?>
                <?php endif; ?>

                <?php if (isset($error) && $error): ?>
                    <div class="alert alert-danger" role="alert">
                        <?= $this->e($error) ?>
                    </div>
                <?php endif; ?>

                <?php if (empty($token)): ?>
                    <div class="alert alert-warning">Lien invalide ou expiré.</div>
                    <a href="/admin/forgot-password" class="btn btn-primary w-100">Demander un nouveau lien</a>
                    <?php return; ?>
                <?php endif; ?>

                <form method="POST" action="/admin/reset-password?token=<?= $this->e($token) ?>">
                    <input type="hidden" name="token" value="<?= $this->e($token) ?>">
                    <div class="form-group mb-3">
                        <label for="password">Nouveau mot de passe</label>
                        <input type="password" class="form-control" id="password" name="password" required
                               minlength="8" autofocus>
                        <small class="form-text text-muted">Au moins 8 caractères.</small>
                    </div>
                    <div class="form-group mb-3">
                        <label for="password_confirm">Confirmer le mot de passe</label>
                        <input type="password" class="form-control" id="password_confirm" name="password_confirm" required
                               minlength="8">
                    </div>
                    <button type="submit" class="btn btn-primary w-100">
                        Réinitialiser le mot de passe
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
