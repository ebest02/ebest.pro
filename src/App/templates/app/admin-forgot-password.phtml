<?php $this->layout('layout::default', ['title' => 'Mot de passe oublié - ebest.pro']) ?>

<div class="row justify-content-center">
    <div class="col-md-6 col-lg-4">
        <div class="card shadow-soft border-light">
            <div class="card-body">
                <h1 class="h3 mb-4 text-center">Mot de passe oublié</h1>

                <?php if (isset($success) && $success): ?>
                    <div class="alert alert-success" role="alert">
                        <?= $this->e($success) ?>
                    </div>
                    <a href="/admin/login" class="btn btn-primary w-100">Retour à la connexion</a>
                    <?php return; ?>
                <?php endif; ?>

                <?php if (isset($error) && $error): ?>
                    <div class="alert alert-danger" role="alert">
                        <?= $this->e($error) ?>
                    </div>
                <?php endif; ?>

                <p class="text-muted small mb-3">
                    Saisissez l’adresse e-mail du compte administrateur. Un lien de réinitialisation vous sera envoyé (valide 1 heure).
                </p>
                <form method="POST" action="/admin/forgot-password">
                    <div class="form-group mb-3">
                        <label for="email">E-mail</label>
                        <input type="email" class="form-control" id="email" name="email" required autofocus>
                    </div>
                    <button type="submit" class="btn btn-primary w-100 mb-2">
                        Envoyer le lien
                    </button>
                    <a href="/admin/login" class="btn btn-link btn-sm p-0">Retour à la connexion</a>
                </form>
            </div>
        </div>
    </div>
</div>
